on: [push]

name: test


jobs:
  CompositeLocal-Custom:
    container: ubuntu:bionic
    runs-on:
      - self-hosted
      - Linux
      - X64

    steps:
      - uses: actions/checkout@v3
      - uses: ./composite
